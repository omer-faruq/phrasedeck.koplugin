local _ = require("gettext")

return {
    name = "phrasedeck",
    fullname = _("PhraseDeck"),
    description = _([[Collect phrases from books and study them with spaced repetition flashcards.]]),
    version = "1.1.0",
}
